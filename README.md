# Galena

Galena is a browser extension that detects how much of a webpage's text is generated by AI. Automatically updates the icon to show a speedometer-based visual (percentage), and clicking the extension on a given webpage highlights text in yellow if it's written in AI.